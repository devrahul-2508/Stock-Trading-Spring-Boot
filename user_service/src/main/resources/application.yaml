spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/stock_db
    username: postgres
    password: rahul2508

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

jwt:
  secret: ThisIsMySuperSecureJWTSecretKeyForStockTradingPlatform2026